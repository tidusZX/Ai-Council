{
  "name": "embeddings-service",
  "version": "0.0.0",
  "private": true,
  "description": "SHAQ OS ingestion worker (yt-dlp + ffmpeg jobs). STUB.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "echo 'no lint configured yet'",
    "test": "echo 'no tests yet'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.20.0",
    "typescript": "^5"
  }
}

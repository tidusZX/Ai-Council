{
  "name": "ingestion-service",
  "version": "0.0.0",
  "private": true,
  "description": "SHAQ OS ingestion worker — yt-dlp + ffmpeg.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js --keep-names --sourcemap=inline",
    "start": "node dist/index.js",
    "lint": "echo 'no lint configured yet'",
    "test": "echo 'no tests yet'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "@shaq-os/database-types": "workspace:*",
    "@shaq-os/supabase-client": "workspace:*",
    "apify-client": "^2.23.3",
    "dotenv": "^16.4.0",
    "hono": "^4.6.0",
    "openai": "^4.71.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "esbuild": "^0.28.0",
    "tsx": "^4.20.0",
    "typescript": "^5"
  }
}
